query MediaTrending($page: Int, $perPage: Int) {
  Page(page: $page, perPage: $perPage) {
    media(sort: TRENDING_DESC) {
      id
      title {
        userPreferred
      }
      coverImage {
        medium
        large
      }
      meanScore
      type
      format
    }
  }
}
