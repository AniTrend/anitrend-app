query StaffDetail($id: Int!) {
  Staff(id: $id) {
    id
    name {
      first
      last
      full
      native
    }
    image {
      large
      medium
    }
    description(asHtml: false)
    language
    isFavourite
    siteUrl
    staffMedia(sort: POPULARITY_DESC) {
      nodes {
        id
        title {
          userPreferred
        }
        type
        format
        coverImage {
          medium
        }
        meanScore
      }
    }
  }
}
