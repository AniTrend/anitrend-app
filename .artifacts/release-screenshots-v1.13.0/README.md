# v1.13.0 review screenshots

Captured on the authenticated `Custom_API_36` emulator. The APKs were installed in place without clearing app data.

- `discover-tags-filter-m3.png`: Discover Anime Tags filter using the new Material 3 bottom sheet.
- `app-general-no-adult.png`: app flavor General settings. The local adult-content preference is absent.
- `github-general-adult-visible.png`: github flavor General settings. The local adult-content preference is visible.

The emulator could not resolve `graphql.anilist.co`, so network-backed content screens were unavailable. These are review artifacts, not replacements for Play listing screenshots that require populated content.
