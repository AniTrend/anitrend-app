<?xml version="1.0" encoding="utf-8"?>
<!-- Settings hub and category screen host. A scrolling surface (lowest
     container) that holds filled section cards (container). The 16dp
     horizontal inset matches the manage-sheet pattern. -->
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurfaceContainerLowest"
    android:clipToPadding="false">

    <LinearLayout
        android:id="@+id/settings_sections"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="@dimen/spacing_xs"
        android:paddingTop="@dimen/spacing_xs"
        android:paddingBottom="@dimen/spacing_sm" />

</androidx.core.widget.NestedScrollView>
