query SeriesListForId($id: Int, $mediaId: Int, $userName: String, $type: MediaType, $status: MediaListStatus, $sort: [MediaListSort]) {
  MediaList(id: $id, userName: $userName, sort: $sort, type: $type, status: $status, mediaId: $mediaId) {
    ... on MediaList {
      id
      mediaId
      status
      score
      progress
      progressVolumes
      repeat
      priority
      private
      notes
      startedAt {
        ... on FuzzyDate {
          year
          month
          day
        }
      }
      completedAt {
        ... on FuzzyDate {
          year
          month
          day
        }
      }
      user {
        ... on User {
          id
          name
          avatar {
            ... on UserAvatar {
              large
              medium
            }
          }
          isFollowing
        }
      }
      media {
        ... on Media {
          id
          title {
            ... on MediaTitle {
              english
              romaji
              native
              userPreferred
            }
          }
          type
          status
          startDate {
            ... on FuzzyDate {
              day
              month
              year
            }
          }
          episodes
          chapters
          volumes
          isAdult
          isFavourite
          nextAiringEpisode {
            ... on AiringSchedule {
              id
              airingAt
              timeUntilAiring
              episode
            }
          }
        }
      }
    }
  }
}
