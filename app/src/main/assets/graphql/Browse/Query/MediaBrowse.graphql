query MediaBrowse($id: Int, $page: Int, $perPage: Int, $type: MediaType, $format: MediaFormat, $startDate: String, $endDate: String, $season: MediaSeason, $genres: [String], $genresExclude: [String], $isAdult: Boolean = false, $sort: [MediaSort], $onList: Boolean, $status: MediaStatus) {
  Page(page: $page, perPage: $perPage) {
    pageInfo {
      ... on PageInfo {
        total
        perPage
        currentPage
        hasNextPage
      }
    }
    media(id: $id, startDate_like: $startDate, endDate_like: $endDate, season: $season, type: $type, format: $format, genre_in: $genres, genre_not_in: $genresExclude, isAdult: $isAdult, sort: $sort, onList: $onList, status: $status) {
      ... on Media {
        id
        title {
          ... on MediaTitle {
            english
            romaji
            native
            userPreferred
          }
        }
        type
        season
        status
        meanScore
        averageScore
        startDate {
          ... on FuzzyDate {
            day
            month
            year
          }
        }
        episodes
        chapters
        volumes
        isAdult
        isFavourite
        nextAiringEpisode {
          ... on AiringSchedule {
            id
            airingAt
            timeUntilAiring
            episode
          }
        }
      }
    }
  }
}
