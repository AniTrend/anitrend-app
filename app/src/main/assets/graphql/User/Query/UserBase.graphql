query UserBase($id: Int, $name: String) {
  User(id: $id, name: $name) {
    ... on User {
      id
      name
      avatar {
        ... on UserAvatar {
          large
          medium
        }
      }
      bannerImage
      isFollowing
    }
  }
}
