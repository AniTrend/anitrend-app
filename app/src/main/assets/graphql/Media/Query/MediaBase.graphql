query MediaBase($id: Int!, $type: MediaType) {
  Media(id: $id, type: $type) {
    ... on Media {
      id
      title {
        userPreferred
      }
      type
      bannerImage
      isFavourite
      mediaListEntry {
        ... on MediaList {
          id
          status
        }
      }
      siteUrl
    }
  }
}
