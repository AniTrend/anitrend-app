query MediaEpisodes($id: Int!, $type: MediaType) {
  Media(id: $id, type: $type) {
    ... on Media {
      externalLinks {
        ... on MediaExternalLink {
          url
          site
        }
      }
    }
  }
}
